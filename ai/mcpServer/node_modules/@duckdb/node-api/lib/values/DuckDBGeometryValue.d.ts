export declare class DuckDBGeometryValue {
    /** In WKB. */
    readonly bytes: Uint8Array;
    /** Takes raw WKB. */
    constructor(bytes: Uint8Array);
    /** Returns WKB in string form matching DuckDB's BLOB-to-VARCHAR. */
    toString(): string;
    /** Takes WKB in string form matching DuckDB's BLOB-to-VARCHAR. */
    static fromString(str: string): DuckDBGeometryValue;
}
/** Takes WKB, either raw or in string form matching DuckDB's BLOB-to-VARCHAR. */
export declare function geometryValue(input: Uint8Array | string): DuckDBGeometryValue;
