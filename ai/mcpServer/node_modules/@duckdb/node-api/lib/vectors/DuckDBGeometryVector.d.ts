import duckdb from '@duckdb/node-bindings';
import { DuckDBGeometryType } from '../DuckDBType';
import { DuckDBGeometryValue } from '../values';
import { DuckDBVector } from './DuckDBVector';
import { DuckDBValidity } from './DuckDBValidity';
export declare class DuckDBGeometryVector extends DuckDBVector<DuckDBGeometryValue> {
    private readonly dataView;
    private readonly validity;
    private readonly vector;
    private readonly itemOffset;
    private readonly _itemCount;
    private readonly itemCache;
    private readonly itemCacheDirty;
    constructor(dataView: DataView, validity: DuckDBValidity, vector: duckdb.Vector, itemOffset: number, itemCount: number);
    static fromRawVector(vector: duckdb.Vector, itemCount: number): DuckDBGeometryVector;
    get type(): DuckDBGeometryType;
    get itemCount(): number;
    getItem(itemIndex: number): DuckDBGeometryValue | null;
    setItem(itemIndex: number, value: DuckDBGeometryValue | null): void;
    flush(): void;
    slice(offset: number, length: number): DuckDBGeometryVector;
}
