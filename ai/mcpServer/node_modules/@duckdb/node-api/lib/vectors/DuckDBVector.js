"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DuckDBVector = void 0;
const node_bindings_1 = __importDefault(require("@duckdb/node-bindings"));
const vectorRegistry_1 = require("./vectorRegistry");
class DuckDBVector {
    static standardSize() {
        return node_bindings_1.default.vector_size();
    }
    static create(vector, itemCount, knownType) {
        if (!vectorRegistry_1.vectorRegistry.create) {
            throw new Error('DuckDBVector factory has not been registered');
        }
        return vectorRegistry_1.vectorRegistry.create(vector, itemCount, knownType);
    }
    toArray() {
        const items = [];
        for (let i = 0; i < this.itemCount; i++) {
            items.push(this.getItem(i));
        }
        return items;
    }
}
exports.DuckDBVector = DuckDBVector;
