"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setBoolean = exports.getBoolean = exports.textDecoder = exports.littleEndian = void 0;
exports.getUInt8 = getUInt8;
exports.getInt16 = getInt16;
exports.getUInt16 = getUInt16;
exports.getInt32 = getInt32;
exports.getUInt32 = getUInt32;
exports.getInt64 = getInt64;
exports.getUInt64 = getUInt64;
exports.getInt128 = getInt128;
exports.getUInt128 = getUInt128;
exports.getStringBytes = getStringBytes;
exports.getString = getString;
exports.getBuffer = getBuffer;
exports.getBigNumFromBytes = getBigNumFromBytes;
exports.getBytesFromBigNum = getBytesFromBigNum;
exports.getBoolean1 = getBoolean1;
exports.getBoolean2 = getBoolean2;
exports.getBoolean4 = getBoolean4;
exports.getBoolean8 = getBoolean8;
exports.makeGetBoolean = makeGetBoolean;
exports.setUInt8 = setUInt8;
exports.setInt16 = setInt16;
exports.setUInt16 = setUInt16;
exports.setInt32 = setInt32;
exports.setUInt32 = setUInt32;
exports.setInt64 = setInt64;
exports.setUInt64 = setUInt64;
exports.setInt128 = setInt128;
exports.setUInt128 = setUInt128;
exports.setBoolean1 = setBoolean1;
exports.setBoolean2 = setBoolean2;
exports.setBoolean4 = setBoolean4;
exports.setBoolean8 = setBoolean8;
exports.makeSetBoolean = makeSetBoolean;
exports.getDecimal16 = getDecimal16;
exports.getDecimal32 = getDecimal32;
exports.getDecimal64 = getDecimal64;
exports.getDecimal128 = getDecimal128;
exports.vectorData = vectorData;
const node_bindings_1 = __importDefault(require("@duckdb/node-bindings"));
const os_1 = __importDefault(require("os"));
const values_1 = require("../values");
exports.littleEndian = os_1.default.endianness() === 'LE';
// function getInt8(dataView: DataView, offset: number): number {
//   return dataView.getInt8(offset);
// }
function getUInt8(dataView, offset) {
    return dataView.getUint8(offset);
}
function getInt16(dataView, offset) {
    return dataView.getInt16(offset, exports.littleEndian);
}
function getUInt16(dataView, offset) {
    return dataView.getUint16(offset, exports.littleEndian);
}
function getInt32(dataView, offset) {
    return dataView.getInt32(offset, exports.littleEndian);
}
function getUInt32(dataView, offset) {
    return dataView.getUint32(offset, exports.littleEndian);
}
function getInt64(dataView, offset) {
    return dataView.getBigInt64(offset, exports.littleEndian);
}
function getUInt64(dataView, offset) {
    return dataView.getBigUint64(offset, exports.littleEndian);
}
// function getFloat32(dataView: DataView, offset: number): number {
//   return dataView.getFloat32(offset, littleEndian);
// }
// function getFloat64(dataView: DataView, offset: number): number {
//   return dataView.getFloat64(offset, littleEndian);
// }
function getInt128(dataView, offset) {
    const lower = getUInt64(dataView, offset);
    const upper = getInt64(dataView, offset + 8);
    return (upper << BigInt(64)) + lower;
}
function getUInt128(dataView, offset) {
    const lower = getUInt64(dataView, offset);
    const upper = getUInt64(dataView, offset + 8);
    return (BigInt.asUintN(64, upper) << BigInt(64)) | BigInt.asUintN(64, lower);
}
function getStringBytes(dataView, offset) {
    const lengthInBytes = dataView.getUint32(offset, true);
    if (lengthInBytes <= 12) {
        return new Uint8Array(dataView.buffer, dataView.byteOffset + offset + 4, lengthInBytes);
    }
    else {
        return node_bindings_1.default.get_data_from_pointer(dataView.buffer, dataView.byteOffset + offset + 8, lengthInBytes);
    }
}
exports.textDecoder = new TextDecoder();
function getString(dataView, offset) {
    const stringBytes = getStringBytes(dataView, offset);
    return exports.textDecoder.decode(stringBytes);
}
function getBuffer(dataView, offset) {
    const stringBytes = getStringBytes(dataView, offset);
    return Buffer.from(stringBytes);
}
function getBigNumFromBytes(bytes) {
    const firstByte = bytes[0];
    const positive = (firstByte & 0x80) > 0;
    const uint64Mask = positive ? 0n : 0xffffffffffffffffn;
    const uint8Mask = positive ? 0 : 0xff;
    const dv = new DataView(// bytes is big endian
    bytes.buffer, bytes.byteOffset + 3, bytes.byteLength - 3);
    const lastUint64Offset = dv.byteLength - 8;
    let offset = 0;
    let result = 0n;
    while (offset <= lastUint64Offset) {
        result = (result << 64n) | (dv.getBigUint64(offset) ^ uint64Mask);
        offset += 8;
    }
    while (offset < dv.byteLength) {
        result = (result << 8n) | BigInt(dv.getUint8(offset) ^ uint8Mask);
        offset += 1;
    }
    return positive ? result : -result;
}
function getBytesFromBigNum(bignum) {
    const numberBytes = []; // little endian
    const negative = bignum < 0;
    if (bignum === 0n) {
        numberBytes.push(0);
    }
    else {
        let vi = bignum < 0 ? -bignum : bignum;
        while (vi !== 0n) {
            numberBytes.push(Number(BigInt.asUintN(8, vi)));
            vi >>= 8n;
        }
    }
    const bigNumBytes = new Uint8Array(3 + numberBytes.length); // big endian
    let header = 0x800000 | numberBytes.length;
    if (negative) {
        header = ~header;
    }
    bigNumBytes[0] = 0xff & (header >> 16);
    bigNumBytes[1] = 0xff & (header >> 8);
    bigNumBytes[2] = 0xff & header;
    for (let i = 0; i < numberBytes.length; i++) {
        const byte = numberBytes[numberBytes.length - 1 - i];
        bigNumBytes[3 + i] = negative ? ~byte : byte;
    }
    return bigNumBytes;
}
function getBoolean1(dataView, offset) {
    return getUInt8(dataView, offset) !== 0;
}
function getBoolean2(dataView, offset) {
    return getUInt16(dataView, offset) !== 0;
}
function getBoolean4(dataView, offset) {
    return getUInt32(dataView, offset) !== 0;
}
function getBoolean8(dataView, offset) {
    return getUInt64(dataView, offset) !== BigInt(0);
}
function makeGetBoolean() {
    switch (node_bindings_1.default.sizeof_bool) {
        case 1:
            return getBoolean1;
        case 2:
            return getBoolean2;
        case 4:
            return getBoolean4;
        case 8:
            return getBoolean8;
        default:
            throw new Error(`Unsupported boolean size: ${node_bindings_1.default.sizeof_bool}`);
    }
}
exports.getBoolean = makeGetBoolean();
// Range checks for fixed-width integers
// Writing a number to a DataView applies the JS ToInt32 and ToUint32 operations,
// which wrap out-of-range values modulo the type's width and truncate fractional
// ones; writing a bigint wraps in the same way. Doing that for data values would
// silently store something other than what the caller supplied, so the setters
// below check first. See duckdb/duckdb-node-neo#469.
//
// The vectors backed by a typed array rather than a DataView repeat these checks
// inline instead of calling them: they sit in the inner loop of every bulk write,
// and calling across a module boundary there costs about 20% of write throughput.
// Keep the two in sync; api/test/integer-ranges.test.ts checks every bound.
function checkUInt8(value) {
    if (!Number.isInteger(value)) {
        throw new Error(`number is not an integer`);
    }
    if (value < 0 || value > 255) {
        throw new Error(`number out of uint8 range`);
    }
    return value;
}
function checkInt16(value) {
    if (!Number.isInteger(value)) {
        throw new Error(`number is not an integer`);
    }
    if (value < -32768 || value > 32767) {
        throw new Error(`number out of int16 range`);
    }
    return value;
}
function checkUInt16(value) {
    if (!Number.isInteger(value)) {
        throw new Error(`number is not an integer`);
    }
    if (value < 0 || value > 65535) {
        throw new Error(`number out of uint16 range`);
    }
    return value;
}
function checkInt32(value) {
    if (!Number.isInteger(value)) {
        throw new Error(`number is not an integer`);
    }
    if (value < -2147483648 || value > 2147483647) {
        throw new Error(`number out of int32 range`);
    }
    return value;
}
function checkUInt32(value) {
    if (!Number.isInteger(value)) {
        throw new Error(`number is not an integer`);
    }
    if (value < 0 || value > 4294967295) {
        throw new Error(`number out of uint32 range`);
    }
    return value;
}
function checkInt64(value) {
    if (BigInt.asIntN(64, value) !== value) {
        throw new Error(`bigint out of int64 range`);
    }
    return value;
}
function checkUInt64(value) {
    if (BigInt.asUintN(64, value) !== value) {
        throw new Error(`bigint out of uint64 range`);
    }
    return value;
}
function checkInt128(value) {
    if (BigInt.asIntN(128, value) !== value) {
        throw new Error(`bigint out of int128 range`);
    }
    return value;
}
function checkUInt128(value) {
    if (BigInt.asUintN(128, value) !== value) {
        throw new Error(`bigint out of uint128 range`);
    }
    return value;
}
// function setInt8(dataView: DataView, offset: number, value: number) {
//   dataView.setInt8(offset, value);
// }
function setUInt8(dataView, offset, value) {
    dataView.setUint8(offset, checkUInt8(value));
}
function setInt16(dataView, offset, value) {
    dataView.setInt16(offset, checkInt16(value), exports.littleEndian);
}
function setUInt16(dataView, offset, value) {
    dataView.setUint16(offset, checkUInt16(value), exports.littleEndian);
}
function setInt32(dataView, offset, value) {
    dataView.setInt32(offset, checkInt32(value), exports.littleEndian);
}
function setUInt32(dataView, offset, value) {
    dataView.setUint32(offset, checkUInt32(value), exports.littleEndian);
}
function setInt64(dataView, offset, value) {
    dataView.setBigInt64(offset, checkInt64(value), exports.littleEndian);
}
function setUInt64(dataView, offset, value) {
    dataView.setBigUint64(offset, checkUInt64(value), exports.littleEndian);
}
function setInt128(dataView, offset, value) {
    checkInt128(value);
    const lower = BigInt.asUintN(64, value);
    const upper = BigInt.asIntN(64, value >> BigInt(64));
    dataView.setBigUint64(offset, lower, exports.littleEndian);
    dataView.setBigInt64(offset + 8, upper, exports.littleEndian);
}
function setUInt128(dataView, offset, value) {
    checkUInt128(value);
    const lower = BigInt.asUintN(64, value);
    const upper = BigInt.asUintN(64, value >> BigInt(64));
    dataView.setBigUint64(offset, lower, exports.littleEndian);
    dataView.setBigUint64(offset + 8, upper, exports.littleEndian);
}
function setBoolean1(dataView, offset, value) {
    setUInt8(dataView, offset, value ? 1 : 0);
}
function setBoolean2(dataView, offset, value) {
    setUInt16(dataView, offset, value ? 1 : 0);
}
function setBoolean4(dataView, offset, value) {
    setUInt32(dataView, offset, value ? 1 : 0);
}
function setBoolean8(dataView, offset, value) {
    setUInt64(dataView, offset, value ? BigInt(1) : BigInt(0));
}
function makeSetBoolean() {
    switch (node_bindings_1.default.sizeof_bool) {
        case 1:
            return setBoolean1;
        case 2:
            return setBoolean2;
        case 4:
            return setBoolean4;
        case 8:
            return setBoolean8;
        default:
            throw new Error(`Unsupported boolean size: ${node_bindings_1.default.sizeof_bool}`);
    }
}
exports.setBoolean = makeSetBoolean();
function getDecimal16(dataView, offset, type) {
    const value = getInt16(dataView, offset);
    return new values_1.DuckDBDecimalValue(BigInt(value), type.width, type.scale);
}
function getDecimal32(dataView, offset, type) {
    const value = getInt32(dataView, offset);
    return new values_1.DuckDBDecimalValue(BigInt(value), type.width, type.scale);
}
function getDecimal64(dataView, offset, type) {
    const value = getInt64(dataView, offset);
    return new values_1.DuckDBDecimalValue(value, type.width, type.scale);
}
function getDecimal128(dataView, offset, type) {
    const value = getInt128(dataView, offset);
    return new values_1.DuckDBDecimalValue(value, type.width, type.scale);
}
function vectorData(vector, byteCount) {
    return node_bindings_1.default.vector_get_data(vector, byteCount);
}
