// packages/utils/sharedUtils.js

// A simple utility function you can reuse across multiple client scripts
export function parseClientData(data) {
    console.log("Shared Utility parsing data natively!");
    return `Parsed: ${data}`;
}
